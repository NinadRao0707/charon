# Core (used by the domain layer — all that's needed to run demo.py and tests)
PyJWT>=2.7.0
cryptography>=42.0.0

# HTTP layer (needed for charon.api.main / uvicorn)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# Dev
pytest>=8.0.0
httpx>=0.27.0
